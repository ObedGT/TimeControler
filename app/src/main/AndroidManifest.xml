<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.parcial.parcialito">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".EventosAceptados"></activity>
        <activity android:name=".PonteEnContacto" />
        <activity android:name=".Notificaciones" />
        <activity android:name=".CuantasHorasLlevo" />
        <activity android:name=".Users" />
        <activity android:name=".Menu" />
        <activity android:name=".Administrador" />
        <activity android:name=".registrar" />
        <activity android:name=".CrearEvento" />
        <activity android:name=".AgregarUsuario" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>